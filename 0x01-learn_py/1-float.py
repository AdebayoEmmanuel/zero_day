#!/usr/bin/python3
# collect input
entry = input('Enter a float: ')
# convert entry from str to float
entry = float(entry)
# print out formatted floating point number rounded up to 2dp
print('Round to 2 decimals: {:.2f}'.format(entry))


